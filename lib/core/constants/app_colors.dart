import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Background
  static const Color background = Color(0xFF050B14);
  static const Color surface = Color(0xFF0E1726);

  // Primary
  static const Color primary = Color(0xFF00B8FF);
  static const Color primaryDark = Color(0xFF0077FF);

  // Accent
  static const Color success = Color(0xFF00E676);
  static const Color warning = Color(0xFFFFC107);
  static const Color danger = Color(0xFFFF4D4F);

  // Text
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB8C7D9);

  // Border
  static const Color border = Color(0xFF1F3A5A);
}
